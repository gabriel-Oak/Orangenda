name: flutter_agenda
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  cupertino_icons: ^0.1.3
  sqflite: ^1.3.0
  bloc: ^6.0.1
  flutter_bloc: ^6.0.1
  path_provider: ^1.6.11
  mask_text_input_formatter: ^1.0.7
  equatable: ^1.2.3
  image_picker: ^0.6.7+4
  url_launcher: ^5.5.0
  flutter_launcher_icons: ^0.7.4

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true

flutter_icons:
  image_path: 'assets/icon/logo180.png'
  android: true
  ios: true


